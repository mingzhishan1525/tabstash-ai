{
  "name": "tabstash-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome Extension MVP for stashing tabs into an AI Inbox and archiving to Notion.",
  "scripts": {
    "install:all": "npm install --workspaces",
    "acceptance": "node scripts/acceptance-test.mjs",
    "validate:real": "npm run build --workspace server && node scripts/real-world-validation.mjs",
    "check:extension-dist": "node scripts/check-extension-build.mjs",
    "scan:secrets": "node scripts/scan-secrets.mjs",
    "release:chrome": "npm run build --workspace extension && node scripts/release-chrome.mjs",
    "build:server": "npm run build --workspace server",
    "start": "npm run start --workspace server",
    "dev:extension": "npm --workspace extension run dev",
    "dev:server": "npm --workspace server run dev",
    "build": "npm --workspaces run build",
    "typecheck": "npm --workspaces run typecheck"
  },
  "workspaces": [
    "extension",
    "server"
  ]
}
